export default class Performance {
    /**
     * User Performance Class
     *
     * @param { number } id
     * @param { array } data
     */
     constructor(id, data) {
        this.userId = id;
        this.data = data;
    }
}